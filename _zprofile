# brew の場所は Apple Silicon (/opt/homebrew) と Intel (/usr/local) で違うので、
# 実際にある方を使う
for _brew in "$HOME/.homebrew/bin/brew" /opt/homebrew/bin/brew /usr/local/bin/brew; do
  if [ -x "$_brew" ]; then
    eval "$("$_brew" shellenv)"
    break
  fi
done
unset _brew
